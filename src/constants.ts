// update these as you like, but ensure:
// - NUM_TOTAL_STORIES is divisible by NUM_PER_PAGE
// - NUM_TOTAL_STORIES is less than or equal to 500
// https://hacker-news.firebaseio.com/v0/topstories.json?print=pretty
export const NUM_TOTAL_STORIES = 100;
export const NUM_PER_PAGE = 25;
